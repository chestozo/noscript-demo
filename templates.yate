module "main"

include "node_modules/noscript/yate/noscript.yate"

match .a ns-view-content {
    params1 = {
        't': 'two'
    }
    params2 = {
        't': 'three'
    }
    url1 = ns-generate-url('index', params1)
    url2 = ns-generate-url('index', params2)

    'A'
    <br/>
    <a href="{ url1 }">go1</a>
    <a href="{ url2 }">go2</a>
}

match .b ns-view-content {
    'B'
}
